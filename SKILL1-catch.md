---
name: catch
description: 读取本地html文件
---

skill_name: catch
skill_type: project
dependencies:
  mcp:
    - File System
    - Sequential thinking
trigger_rule: 用户要求读取指定html、读取网页模板、以本地html文件作为排版参考时自动启用
template_dir: ./website/

# 一、核心任务
根据用户提供的HTML文件名，读取项目website文件夹内**单独一份指定.html模板文档**，提取完整页面排版结构、布局样式、文字模块格式，仅作为模板参考素材缓存，不生成新文件、不自动做攻略。

# 二、标准执行步骤
1. 获取用户指定模板文件名
确认需要读取的html文档名称，拼接完整路径 ./website/文件名.html
2. 文件校验
调用File System检索该路径，判断文件是否存在；不存在立刻提示模板文件缺失
3. 读取完整模板源码
完整读取这份指定HTML的全部代码，保留布局、排版、区块划分结构，不清洗标签
4. 提取排版参考信息
拆分页面结构：标题区块、图文布局、段落格式、模块分区、图片摆放规则，整理模板排版规范
5. 缓存模板结构
将这份HTML的完整排版样式缓存，供后续其他技能参考套用该模板格式

# 三、强制约束规则
1. 只读取用户指定**单个html文件**，不批量遍历全部网页；
2. 完整保留HTML排版结构，不删除布局相关标签，保证能用作格式模板；
3. 仅做读取、解析模板布局，不爬取线上网页、不自动生成任何新文档；
4. 仅访问项目内 ./website/ 目录，禁止读取电脑其他路径文件；
5. 解析完成后停止执行，等待下一步指令套用该模板。

指令启用与另外两个配套使用